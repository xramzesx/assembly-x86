DATA1 SEGMENT
T1	DB "To je tekst 8 D $"
DATA1 ENDS

CODE1 SEGMENT
START1:
	MOV AX, SEG STOS
	MOV SS, AX
	MOV SP, OFFSET WSTOS1

	MOV AX, SEG T1
	MOV DS, AX
	MOV DX, OFFSET T1
	MOV AH, 9
	INT 21h

	MOV AL, 0
	MOV AH, 4CH ; end program
	INT 21h

CODE1 ENDS

STOS1 SEGMENT STACK
	DW 300 DUP(?)
WSTOS1	DW ?
STOS1 ENDS